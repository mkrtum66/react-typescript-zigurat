import styled from 'styled-components';

export const ConfirmPayPopupWrapper = styled.div`
  .confirm-pay-modal-wrapper {
    .ant-modal-title {
      font-weight: 500;
      font-size: 21px;
    }
    .ant-modal-body {
      padding-top: 0;
      span {
        font-weight: 500;
        color: var(--text-primary);
      }
    }
  }
`;
